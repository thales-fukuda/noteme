import styled from 'styled-components';

const TitleText = styled.textarea`
  height: 40px;
  font-family: 'Lato';
  font-size: 32px;
  font-weight: bold;
  :focus{
    outline: none;
  }
`;

export default TitleText;
